### 회원들을 나이가 증가하는 순으로, 나이가 같으면 먼저 가입한 사람이 앞에 오는 순서로 정렬 ###

n = int(input())
lst_age = []
students = {}
for i in range(n):
    age, name = input().split()
    age = int(age)
    if age not in lst_age:
        lst_age.append(age)
    if age in students:
        students[age].append(name)
    else:
        students[age] = [name]
    
lst_age.sort()
for age in lst_age:
    for name in students[age]:
        print(age, name)
